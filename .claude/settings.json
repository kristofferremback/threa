{
  "enabledPlugins": {},
  "plansDirectory": ".claude/plans",
  "hooks": {
    "SessionStart": [
      {
        "command": "[ -f /tmp/.claude-code-web ] && { (npm install --prefer-offline 2>/dev/null || npm install); bash scripts/lazy-setup.sh &>/tmp/lazy-setup.log & }; exit 0",
        "timeout": 180
      }
    ]
  }
}
