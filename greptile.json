{
  "enabled": true,
  "strictness": 2,
  "commentTypes": ["logic", "syntax"],
  "triggerOnUpdates": true,
  "statusCheck": true,
  "excludeAuthors": ["dependabot[bot]", "renovate[bot]"],
  "ignorePatterns": "*.generated.*\n**/node_modules/**\npackage-lock.json\nbun.lockb\ndist/**\ndev-dist/**\n.env*\ntest-results/**\nplaywright-report/**\n*.log\ngreptile.json",
  "instructions": "Architecture, invariants, and the full app inventory live in CLAUDE.md and docs/system-overview.md (both already loaded as customContext.files below). Treat those as the source of truth — do not rely on a frozen description in this file. The structured rules under customContext.rules below cover the most frequently-violated invariants with file-glob scopes; CLAUDE.md is the complete reference.\n\nWhat NOT to flag:\n- Pre-existing issues not introduced by this PR\n- Issues that TypeScript compilation or ESLint would catch (types, imports, lint)\n- Stylistic preferences without a concrete rule violation in CLAUDE.md\n- Theoretical risks or hypothetical edge cases without evidence of exploitability\n- General best practice suggestions that don't map to a specific project rule\n\nSecurity calibration:\n- React JSX is safe from XSS unless dangerouslySetInnerHTML is used\n- ULIDs/UUIDs are cryptographically unguessable — do not flag as enumeration risks\n- Environment variables are trusted — do not flag as hardcoded secrets\n- DoS, rate limiting, log spoofing, regex complexity, missing audit logs, and outdated dependency warnings are out of scope\n\nPlan adherence:\nIf plan files exist in .claude/plans/, check that PR changes align with the plan. Flag missing corresponding changes: API change without frontend/backoffice update, type change without usage update, schema change without migration.",
  "summarySection": {
    "included": true,
    "collapsible": true,
    "defaultOpen": false
  },
  "issuesTableSection": {
    "included": true,
    "collapsible": true,
    "defaultOpen": false
  },
  "confidenceScoreSection": {
    "included": true
  },
  "sequenceDiagramSection": {
    "included": true,
    "collapsible": true,
    "defaultOpen": false
  },
  "customContext": {
    "rules": [
      {
        "scope": ["**/migrations/**"],
        "rule": "No foreign key constraints in migrations. Referential integrity is enforced in application code, not PostgreSQL schema. (INV-1)"
      },
      {
        "scope": ["**/migrations/**"],
        "rule": "No DB enums. Use TEXT columns and validate in application code. (INV-3)"
      },
      {
        "scope": ["apps/backend/src/**"],
        "rule": "Real-time event delivery must go through the outbox pattern. Do not publish events directly via socket.io emit or Redis pub/sub outside of the outbox dispatcher. (INV-4)"
      },
      {
        "scope": ["**/migrations/**"],
        "rule": "Migrations are append-only. Never edit or delete existing migration files. Only add new ones. (INV-17)"
      },
      {
        "scope": ["apps/frontend/src/**/*.tsx", "apps/backoffice/src/**/*.tsx"],
        "rule": "Do not define React components inside other React components. Extract to separate functions or files. (INV-18)"
      },
      {
        "scope": ["apps/backend/src/**", "apps/control-plane/src/**", "packages/backend-common/src/**"],
        "rule": "Never do select-then-update without locking or concurrency control. Use ON CONFLICT, advisory locks, or transactions with row locks for write paths. (INV-20)"
      },
      {
        "scope": ["apps/backend/src/**"],
        "rule": "All AI/LLM usage must go through the project AI wrapper (createAI), not raw SDK imports from @anthropic-ai/sdk or openai. (INV-28)"
      },
      {
        "scope": ["apps/backend/src/**", "apps/control-plane/src/**", "packages/backend-common/src/**"],
        "rule": "Avoid withClient for single-query paths. Pass pool directly instead of acquiring a dedicated client. (INV-30)"
      },
      {
        "scope": ["apps/backend/src/**"],
        "rule": "Do not keep database connections open during slow AI or network calls. Release the connection first, then do the slow work. (INV-41)"
      },
      {
        "scope": ["apps/backend/src/**", "apps/control-plane/src/**"],
        "rule": "Validate API inputs (body, query, params) with Zod schemas, not manual typeof checks. (INV-55)"
      }
    ],
    "files": [
      {
        "scope": [],
        "path": "CLAUDE.md",
        "description": "Authoritative project invariants, architecture rules, coding standards, and testing expectations. Primary review specification — read this first."
      },
      {
        "scope": [],
        "path": "docs/system-overview.md",
        "description": "How the apps in this monorepo interact: their responsibilities, infrastructure dependencies, and deployment targets."
      },
      {
        "scope": [],
        "path": "docs/deployment.md",
        "description": "Production and staging deploy topology, env vars per service, the workspace COPY discipline that the Dockerfile lint enforces, and rollback steps."
      },
      {
        "scope": [],
        "path": "docs/model-reference.md",
        "description": "Approved AI models and their configurations. AI code must only use models listed here (INV-16)."
      }
    ],
    "other": []
  }
}
