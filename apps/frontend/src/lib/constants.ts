/** Number of events fetched per page (bootstrap and pagination). */
export const EVENT_PAGE_SIZE = 50

/** Fetch next page after scrolling past this fraction of the current page. */
export const SCROLL_FETCH_RATIO = 0.5
